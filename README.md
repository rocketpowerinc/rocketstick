# rocketstick📦 Overview

### Quick Start
- Run `.1-initialize-usb.sh` on a new usb stick
- unhide files with `ctrl + h`
- Run `.2-initialize-linux.sh` to copy locally
- Use `./refresh-files.sh to update (will not work in offline mode)